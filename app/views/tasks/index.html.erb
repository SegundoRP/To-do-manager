<h1>Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
  <li><%= link_to task.title, task_path(task) %> - <%= link_to "Borrar tarea", task_path(task),
  method: :delete, data: {confirm: "¿Está seguro de eliminar la tarea?"} %></li>
  <% end %>
</ul>
<br>
<%= link_to "Agregar tarea", tasks_new_path %>
